@model ICollection<FPTBookStore.Models.Author>

<div class="row">
    <div class="col-md-8 offset-md-2">
            <h5 class="text-secondary">Total Authors: @ViewData["total"]</h5>
        <table class="table">
            <thead>
                <tr class="text-center">
                    <th>Id</th>
                    <th>Name</th>
                    @if (User.Identity.IsAuthenticated && User.IsInRole("StoreOwner"))
                    {
                        <th>Actions</th>
                    }
                </tr>
            </thead>
            <tbody>
                @foreach (var a in Model)
               {
                   <tr class="text-center">
                       <td class="align-middle">@a.Id</td>
                       <td class="align-middle">@a.LName @a.FName</td>
                       
                            <td class="align-middle">
                                <a class="btn btn-outline-info" asp-action="Detail" asp-route-id="@a.Id"><i class="bi bi-eye"></i>Detail</a>
                                <a class="btn btn-outline-warning" asp-action="Edit" asp-route-id="@a.Id"><i class="bi bi-pencil-square"></i>Edit</a>
                                <a class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@a.Id"><i class="bi bi-trash"></i>Delete</a>
                            </td>
                       
                    </tr>
               }   
            </tbody>
        </table>     
        <div style="text-align:center">
             <a class="btn btn-outline-success" style="text-align:center" asp-action="Create">Add New Author</a>
        </div>
    </div>
</div>

